# PowerShell Desired State Configuration

DSC provides continuous enforcement for durable Windows state. Configurations compile to MOF files in a controlled pipeline; MOF files and secrets are not committed to public repositories. The Local Configuration Manager operates in ApplyAndAutoCorrect mode in the lab and reports compliance every 30 minutes.
