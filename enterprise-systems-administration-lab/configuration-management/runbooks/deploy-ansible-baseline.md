# Deploy Ansible Baseline

Run syntax check, check mode on WEB01, capture pre-change state, execute site.yml in serial batches, run validate.yml, rerun site.yml for idempotency, attach evidence, and close the change only after service-owner confirmation.

## Completion evidence
Record command, operator, timestamps, affected systems, results, exceptions, and ticket reference.
