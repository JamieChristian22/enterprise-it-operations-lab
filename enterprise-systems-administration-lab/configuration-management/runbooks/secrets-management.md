# Secrets Management

Store secrets in Ansible Vault or a credential platform, grant least privilege, rotate automation credentials every 90 days, block secrets in logs, scan commits, and revoke credentials immediately after suspected exposure.

## Completion evidence
Record command, operator, timestamps, affected systems, results, exceptions, and ticket reference.
