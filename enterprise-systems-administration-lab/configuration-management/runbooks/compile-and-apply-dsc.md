# Compile And Apply Dsc

Validate required modules, compile configurations to a protected output directory, inspect MOF metadata for secrets, apply LCM settings, run Start-DscConfiguration with Wait and Verbose, then confirm Test-DscConfiguration returns True.

## Completion evidence
Record command, operator, timestamps, affected systems, results, exceptions, and ticket reference.
