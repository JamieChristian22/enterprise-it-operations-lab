# Failed Deployment Response

Pause the batch, identify failed hosts, compare successful and failed task output, determine whether the issue is code, dependency, access, or host-specific, roll back affected hosts, and open an incident if service is degraded.

## Completion evidence
Record command, operator, timestamps, affected systems, results, exceptions, and ticket reference.
