# DNS Failover Retest

The recovery DNS alias was activated with a 300-second TTL. Test clients resolved the recovery service within six minutes. Authentication, file access, and application access all passed. The previous delay was eliminated.
