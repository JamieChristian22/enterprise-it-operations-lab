# Security Policy

Do not submit real credentials, tenant IDs, access tokens, MFA secrets, customer data, private IP inventories, or unredacted production logs.

Report accidental data exposure by removing the content from the repository history and rotating any affected credentials immediately.

The included scripts are read-only diagnostic tools. Review all scripts before running them in an organizational environment.
