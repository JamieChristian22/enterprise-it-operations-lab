# Deployment Guide
Use short-lived credentials, verify account and subscription, run repository validation, review the Terraform plan, obtain change approval, apply during the approved window, capture outputs, test the health endpoints, verify alarms and backups, and destroy the lab when complete. Never commit state files or secrets.
