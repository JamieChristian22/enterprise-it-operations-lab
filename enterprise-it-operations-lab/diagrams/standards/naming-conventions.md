# Naming Conventions

- Use lowercase kebab-case filenames.
- Use platform or process name first.
- Use `-flow` for operational workflows.
- Use `-architecture` for system designs.
- Use `-topology` for identity or network relationships.
- Use `-map` for dependencies.
